/*
check BR+CS use case.
A read-only JS object available on the client (browser) that carries data computed on the server during form load.
It’s populated by a Display Business Rule (runs before the form renders) and then accessible in Client Scripts/UI Policies via g_scratchpad.someField
*/
